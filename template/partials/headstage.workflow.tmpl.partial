<h2 id="workflow">Workflow</h2>
<p>This is a fully functional workflow that configures all devices on a {{headstage}} headstage.</p>
<div class="workflow"> 
    <p><img src="{{workflow_file}}" alt="{{title}}"></p>
</div>